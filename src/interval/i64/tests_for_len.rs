// -----------------------------------------------------------------------------
// @generated by xtask/codegen (signed)
// DO NOT EDIT MANUALLY.
// Changes will be overwritten.
// -----------------------------------------------------------------------------

use super::*;

const MIN: i64 = i64::MIN;
const MAX: i64 = i64::MAX;
const NEG_ONE: i64 = -1;
const ZERO: i64 = 0;
const ONE: i64 = 1;

mod unit_tests {
    use super::*;

    #[test]
    fn len_of_singleton_is_one() {
        let iv = I64CO::try_new(ZERO, ONE).unwrap();
        assert_eq!(iv.len(), 1);

        let iv = I64CO::try_new(MIN, MIN + ONE).unwrap();
        assert_eq!(iv.len(), 1);

        let iv = I64CO::try_new(MAX - ONE, MAX).unwrap();
        assert_eq!(iv.len(), 1);
    }

    #[test]
    fn len_matches_basic_examples() {
        let cases = [
            (MIN, MIN + ONE, 1),
            (MIN, MIN + 8, 8),
            (-5, ZERO, 5),
            (ZERO, ONE, 1),
            (ZERO, 10, 10),
            (5, 9, 4),
            (MAX - ONE, MAX, 1),
            (MIN, MAX, u64::MAX),
        ];

        for (start, end_excl, expect) in cases {
            let iv = I64CO::try_new(start, end_excl).unwrap();
            assert_eq!(iv.len(), expect, "case [{start}, {end_excl})");
        }
    }

    #[test]
    fn len_matches_iteration_count() {
        let cases = [
            (MIN, MIN + ONE),
            (MIN, MIN + 8),
            (-100, -90),
            (-5, ZERO),
            (NEG_ONE, ONE),
            (ZERO, ONE),
            (ZERO, 10),
            (10, 20),
            (MAX - ONE, MAX),
        ];

        for (start, end_excl) in cases {
            let iv = I64CO::try_new(start, end_excl).unwrap();
            assert_eq!(
                iv.len(),
                iv.iter().count() as u64,
                "case [{start}, {end_excl})"
            );
        }
    }

    #[test]
    fn len_equals_end_minus_start_in_order_space() {
        const SIGN_MASK: u64 = 1u64 << (i64::BITS - 1);

        let cases = [
            (MIN, MIN + ONE),
            (MIN, NEG_ONE),
            (MIN, ZERO),
            (MIN, MAX),
            (NEG_ONE, ZERO),
            (NEG_ONE, ONE),
            (ZERO, ONE),
            (ZERO, MAX),
            (MAX - ONE, MAX),
        ];

        for (start, end_excl) in cases {
            let iv = I64CO::try_new(start, end_excl).unwrap();

            let expect = ((end_excl as u64) ^ SIGN_MASK) - ((start as u64) ^ SIGN_MASK);

            assert_eq!(iv.len(), expect, "case [{start}, {end_excl})");
        }
    }

    #[test]
    fn len_is_monotonic_under_interval_extension() {
        let small = I64CO::try_new(-5, 5).unwrap();
        let large = I64CO::try_new(-10, 10).unwrap();

        assert!(small.len() < large.len());

        let small = I64CO::try_new(MIN, -120).unwrap();
        let large = I64CO::try_new(MIN, -100).unwrap();

        assert!(small.len() < large.len());

        let small = I64CO::try_new(100, 110).unwrap();
        let large = I64CO::try_new(100, MAX).unwrap();

        assert!(small.len() < large.len());
    }

    #[test]
    fn len_of_maximal_i64_interval_is_255() {
        let iv = I64CO::try_new(MIN, MAX).unwrap();
        assert_eq!(iv.len(), u64::MAX);
    }
}

mod prop_tests {
    use proptest::prelude::*;

    use super::*;

    proptest! {
        #[test]
        fn prop_len_is_positive_for_valid_intervals(start in any::<i64>(), end in any::<i64>()) {
            if let Some(iv) = I64CO::try_new(start, end) {
                prop_assert!(iv.len() >= 1);
            }
        }

        #[test]
        fn prop_len_matches_order_space_difference(start in any::<i64>(), end in any::<i64>()) {
            if let Some(iv) = I64CO::try_new(start, end) {
                const SIGN_MASK: u64 = 1u64 << (i64::BITS - 1);
                let expect = ((end as u64) ^ SIGN_MASK) - ((start as u64) ^ SIGN_MASK);
                prop_assert_eq!(iv.len(), expect);
            }
        }
    }
}
